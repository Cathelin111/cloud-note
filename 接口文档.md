# 云笔记项目接口文档

- 技术栈：Spring MVC + MyBatis + MySQL/H2
- 所有接口均返回 JSON，统一结构：`NoteResult<T>` 泛型
- 访问前缀：`http://localhost:8080/cloud_note`
- 所有请求均为 `POST`，参数为 `application/x-www-form-urlencoded`

## 系统角色与登录入口

| 角色 | 说明 | 入口 |
|---|---|---|
| 游客 | 未登录用户，可浏览公开分享笔记与活动信息；收藏/投稿/顶踩等操作需登录 | 登录页“游客浏览”→ `guest.html` 分享广场；`activity.html` 活动页对游客开放 |
| 普通用户 | 注册即生效（状态 normal），登录后进入 `edit.html` 使用完整功能 | 登录页账号登录（`user/login.do`） |
| 系统管理员 | 预置账号 `admin / admin123`（`cn_user.cn_user_role='admin'`），管理用户、分享内容与活动 | 登录页“管理员登录”→ 后台 `admin.html` |

- 用户状态 `cn_user_status`：`normal` 正常、`disabled` 停用（停用后无法登录，见接口 2）
- 分享状态 `cn_share_status`：`normal` 公开可见、`disabled` 已下架（下架后前台搜索/详情/收藏均不可用）
- 后台接口统一携带操作者 `userId`（管理员自己的用户ID），服务端校验其角色为启用的管理员，否则返回 `status=4`

## 统一返回结构

```json
{
  "status": 0,        // 0=成功；1/2/3=业务失败；4=无管理员权限
  "msg": "操作结果描述",
  "data": { ... }     // 业务数据，失败时为 null
}
```

---

## 一、用户模块 `/user/*`

### 1. 注册 `POST /user/add.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| name | String | 用户名（必填） |
| password | String | 密码（必填，MD5+Base64 加密后入库） |
| nick | String | 昵称 |

- `status=0` 注册成功（注册即生效：角色 user、状态 normal、记录注册时间）；`status=1` 用户名已被占用

### 2. 登录 `POST /user/login.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| name | String | 用户名 |
| password | String | 密码 |

- `status=0` 成功，`data` 为 User 对象（`cn_user_id`、`cn_user_name`、`cn_user_nick`、`cn_user_role`、`cn_user_status`）
- `status=1` 用户名不存在；`status=2` 密码错误；`status=3` 账号已被管理员停用
- 前端登录成功后把 `userId`、`userName`、`userNick`、`userRole` 写入 Cookie（2 小时）
- 前端按角色跳转：`role=admin` → `admin.html` 后台；普通用户 → `edit.html`

### 3. 修改密码 `POST /user/change.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| userName | String | 用户名 |
| last_password | String | 原密码 |
| final_password | String | 新密码 |

- `status=0` 成功；`status=1` 原密码不正确；`status=2` 新密码与原密码一致

---

## 二、笔记本模块 `/book/*`、`/notebook/*`

### 4. 加载正常笔记本列表 `POST /book/loadBooks.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| userId | String | 用户ID |

- 只返回类型为 normal 的笔记本，按创建时间倒序；`data` 为 Book 数组

### 5. 创建笔记本 `POST /book/add.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| userId | String | 用户ID |
| title | String | 笔记本名称 |

- 自动创建类型为 normal 的笔记本，返回创建的 Book

### 6. 查询特殊笔记本 `POST /notebook/findSpecial.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| userId | String | 用户ID |

- 返回 `data` 为 Map：`{ "favorites": Book, "recycle": Book, "action": Book }`
- 特殊笔记本（收藏/回收站/活动）**不存在时自动创建**

### 7. 查询笔记本列表（活动投稿选择用） `POST /notebook/findList.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| userId | String | 用户ID |

- 与 `loadBooks.do` 相同，返回正常笔记本列表

### 8. 修改笔记本名称 `POST /notebook/updateName.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| notebookId | String | 笔记本ID |
| name | String | 新名称 |

### 9. 彻底删除笔记本 `POST /notebook/delete.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| notebookId | String | 笔记本ID |

- 注意：前端会拦截"笔记本下仍有笔记"的情况；物理删除不可恢复

---

## 三、笔记模块 `/note/*`

### 10. 加载笔记本下的笔记列表 `POST /note/loadnotes.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| bookId | String | 笔记本ID |

- `data` 为 Map 数组：`[{cn_note_id, cn_note_title}]`，只返回 `status_id='1'`（正常）的笔记

### 11. 加载笔记详情 `POST /note/load.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| noteId | String | 笔记ID |

- `data` 为 Note 完整对象

### 12. 创建笔记 `POST /note/add.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| userId | String | 用户ID |
| bookId | String | 笔记本ID |
| title | String | 笔记标题 |

- 状态约定：`status_id=1`(normal)、`type_id=1`(normal)

### 13. 保存笔记 `POST /note/update.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| noteId | String | 笔记ID |
| title | String | 标题 |
| body | String | 内容（UEditor 富文本 HTML） |

### 14. 删除笔记（移入回收站） `POST /note/delete.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| noteId | String | 笔记ID |

- 逻辑：把笔记移入用户的"回收站"特殊笔记本（不存在时自动创建），非物理删除

### 15. 移动笔记 `POST /note/move.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| noteId | String | 笔记ID |
| bookId | String | 目标笔记本ID |

- 同时用于"转移笔记"与"回收站恢复笔记"

### 16. 彻底删除笔记 `POST /note/deleteRecycle.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| noteId | String | 笔记ID |

- 回收站中的物理删除，不可恢复

### 17. 查询笔记本中的笔记（活动投稿选择用） `POST /note/findNote.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| noteBookId | String | 笔记本ID |

- 与 `loadnotes.do` 返回相同结构

---

## 四、分享模块 `/share/*`、`/note/*`

### 18. 分享笔记 `POST /share/add.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| noteId | String | 笔记ID |

- 把笔记标题+内容**复制**到 `cn_share` 分享表

### 19. 搜索分享笔记 `POST /share/search.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| keyword | String | 关键字（标题模糊匹配 LIKE %keyword%） |

- 无需登录；**只返回公开可见（状态 normal）的分享**
- `data` 为 Share 数组（`cn_share_id`、`cn_share_title`、`cn_share_body`、`cn_share_author` 分享人昵称）
- 注：按标题匹配并返回全部结果；**前台页面现推荐使用下面 19.1 的分页搜索接口**

### 19.1 分享广场搜索(分页) `POST /share/searchPage.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| keyword | String | 关键字（可为空=浏览全部）；**标题或正文命中**（LIKE 模糊） |
| page | Integer | 页码，默认 1；每页 10 条 |

- 无需登录；只返回公开可见（normal）的分享；`data = { total, page, size, rows: [Share...] }`，rows 含分享人昵称 `cn_share_author`
- 登录用户的"我的笔记"(edit.html)顶部搜索与 `guest.html` 分享广场均使用本接口

### 20. 查看分享笔记详情 `POST /note/load_share.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| shareId | String | 分享ID |

- 无需登录；`status=1` 分享不存在、`status=2` 分享已被管理员下架（不可查看）

### 21. 收藏分享笔记 `POST /note/likeShareNote.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| shareId | String | 分享ID |
| userId | String | 用户ID |

- 把分享内容存为一条笔记（`type_id=2` 收藏）放入用户"收藏"笔记本（不存在时自动创建）

---

## 五、活动模块 `/activity/*`、`/note/*`

### 22. 查询活动列表 `POST /activity/findActivity.do`

- 无参数；`data` 为 Activity 数组（`cn_activity_id`、`cn_activity_title`、`cn_activity_body`、`cn_activity_end_time`）

### 23. 分页查询活动投稿 `POST /activity/findNoteActivity.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| activityId | String | 活动ID |
| currentPage | Integer | 页码（从1开始，默认1） |

- 每页 10 条（PAGE_SIZE），按投稿标题排序；`data` 为 NoteActivity 数组

### 24. 查询投稿详情 `POST /activity/findNoteActivityDetail.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| noteActivityId | String | 投稿ID |

### 25. 参加活动（投稿） `POST /activity/addNoteActivity.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| activityId | String | 活动ID |
| noteId | String | 笔记ID |
| userId | String | 用户ID |

- 把笔记投稿到活动（存入 `cn_note_activity`，顶/踩初始为0），并把笔记**复制一份**到用户"活动"笔记本（不存在时自动创建）

### 26. 顶投稿 `POST /activity/upNoteActivity.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| noteActivityId | String | 投稿ID |

- 投稿顶数 +1

### 27. 踩投稿 `POST /activity/downNoteActivity.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| noteActivityId | String | 投稿ID |

- 投稿踩数 +1

### 28. 收藏活动投稿 `POST /note/likeActivityNote.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| noteActivityId | String | 投稿ID |
| userId | String | 用户ID |

- 把投稿标题+内容存为一条笔记（`type_id=2` 收藏）放入用户"收藏"笔记本

---

## 六、后台管理模块 `/admin/*`（系统管理员）

所有接口参数均含 `userId`（=管理员自己的用户ID）。非管理员调用一律返回 `status=4 无管理员权限`。
后台页面：`admin.html`（管理员登录后跳转），数据每页 10 条。

### 29. 用户管理-分页查询 `POST /admin/user/list.do`

| 参数 | 类型 | 说明 |
|---|---|---|
| userId | String | 管理员ID |
| keyword | String | 可选，按用户名/昵称模糊查询 |
| page | Integer | 页码，默认 1 |

- `data = { total, page, size, rows: [User...] }`，rows 仅含普通用户（role=user），User 含 role/status/注册时间

### 30. 用户管理-停用/启用 `POST /admin/user/updateStatus.do`

参数：`userId`、`targetId`（目标用户）、`status`（normal 启用 / disabled 停用）
- 停用后该用户无法登录；`status=3` 不允许操作管理员账号

### 31. 用户管理-删除用户 `POST /admin/user/delete.do`

参数：`userId`、`targetId`
- 级联清理：该用户全部笔记本→笔记→其分享记录→活动投稿记录；`status=3` 不允许删除管理员账号

### 32. 分享管理-分页查询 `POST /admin/share/list.do`

参数：`userId`、`keyword`（可选，按标题模糊）、`page`
- `data = { total, page, size, rows: [Share...] }`；rows 含已下架分享与分享人 `cn_share_author`

### 33. 分享管理-下架/上架 `POST /admin/share/updateStatus.do`

参数：`userId`、`shareId`、`status`（normal 上架 / disabled 下架）
- 下架后：前台 `share/search.do` 不再返回、`note/load_share.do` 与收藏接口拒绝访问

### 34. 分享管理-删除分享 `POST /admin/share/delete.do`

参数：`userId`、`shareId`

### 35. 活动管理-活动列表 `POST /admin/activity/list.do`

参数：`userId`；`data` 为 Activity 数组（含 id/title/body/end_time）

### 36. 活动管理-发布/修改活动 `POST /admin/activity/save.do`

参数：`userId`、`activityId`（为空=新建）、`title`（必填）、`body`、`endTime`（毫秒时间戳，空=长期有效）

### 37. 活动管理-删除活动 `POST /admin/activity/delete.do`

参数：`userId`、`activityId`；该活动下全部投稿一并删除

### 38. 管理员管理-提升用户为管理员 `POST /admin/user/setAdmin.do`

参数：`userId`、`targetId`（目标普通用户）
- 把普通用户角色改为 admin（同时恢复其账号状态为 normal）；`status=3` 目标已是管理员
- 提升后该用户用登录页「管理员登录」入口进入后台

### 39. 管理员管理-直接新建管理员账号 `POST /admin/user/addAdmin.do`

参数：`userId`、`name`（用户名，必填且不可重复）、`password`（≥6位）、`nick`（可选，默认"管理员"）
- 直接创建 `role=admin` 的新账号（密码按 MD5+Base64 存储）

### 40. 管理员管理-查询全部管理员 `POST /admin/user/admins.do`

参数：`userId`；`data` 为 User 数组（role=admin），用于后台"管理员管理"面板展示

### 41. 系统-立即备份数据库 `POST /admin/system/backup.do`

参数：`userId`（管理员ID）
- 对 H2 执行在线 `SCRIPT TO`，在 `数据目录/backup` 生成带时间戳的完整 SQL 快照；`msg` 返回文件路径
- 每次启动应用会自动执行一次当日备份（同一天不重复）；备份只保留最近 15 份
- MySQL 环境请使用 mysqldump（返回 `status=1` 提示）

### 42. 数据-用户数据导出(同步预留) `POST /data/export.do`

参数：`userId`
- 导出该用户全部数据 JSON：`{ exportVersion, exportedAt, user(不含密码), books[{...,notes:[...]}], shares[], noteActivities[] }`
- 主键均为稳定 UUID，供未来"导入+合并"实现跨设备同步；前端入口：我的笔记→头像菜单→"导出我的数据"
- 详见仓库根目录《数据持久化与同步说明.md》

---

## 附：数据库约定

- 笔记状态 `cn_note_status_id`：1=normal（正常）、2=delete（备用）
- 笔记类型 `cn_note_type_id`：1=normal、2=favor（收藏）、3=share（备用）
- 笔记本类型 `cn_notebook_type_code`：normal（正常）、favorites（收藏）、recycle（回收站）、action（活动）、push（推送）
- 用户角色 `cn_user_role`：user（普通用户）、admin（系统管理员）
- 用户状态 `cn_user_status`：normal（正常）、disabled（停用）
- 分享状态 `cn_share_status`：normal（公开）、disabled（已下架）
- 主键：应用层生成，UUID 去除横线（`NoteUtil.createId()`）
- 密码：MD5 摘要 + Base64 编码存储
- 预置账号：管理员 `admin/admin123`；演示普通用户 `demo/123456`

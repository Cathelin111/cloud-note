# 云笔记部署指南（从 GitHub 克隆后如何本地运行）

仓库地址：https://github.com/Cathelin111/cloud-note
技术栈：Spring MVC + MyBatis + jQuery 前端；数据库支持 **MySQL** 或 **H2 嵌入式**（二选一）。

## 一、准备环境

| 依赖 | 版本建议 |
|---|---|
| JDK | 1.8 |
| Maven | 3.x（或用 IDE 自带） |
| Tomcat | 8.5 / 9（IDE 内置 Tomcat 也可以） |
| 数据库 | 方案A：MySQL 5.7+；方案B：无需安装（H2 嵌入式） |

> 说明：仓库里 `cloud_note/src/main/resources/jdbc.properties` 默认是 **H2** 配置（打包免装 MySQL 用）。
> 想用 MySQL 的请看方案 A 第 2 步切换。

---

## 二、方案 A：MySQL（与课程环境一致，多设备可用共享库）

1. 克隆并导入：
   ```bash
   git clone https://github.com/Cathelin111/cloud-note.git
   ```
   用 IDEA/Eclipse 以 **Maven 项目**导入 `cloud_note` 子目录。

2. 准备数据库：
   - 在 MySQL 中创建库：`CREATE DATABASE cloud_note DEFAULT CHARACTER SET utf8;`
   - 导入仓库根目录的 **`cloud_note.sql`**（含全部表、字典和种子数据：管理员 admin/admin123、演示用户 demo/123456）。
   - ⚠️ 重要：MySQL 必须**先导入 cloud_note.sql**，不要留空库启动，否则系统会用 H2 语法的建表脚本建库导致失败（建表脚本只面向 H2）。

3. 切换数据库连接：把
   `cloud_note/src/main/resources/jdbc.properties` 改成：
   ```properties
   db.driver=com.mysql.jdbc.Driver
   db.url=jdbc:mysql://localhost:3306/cloud_note?useUnicode=true&characterEncoding=utf8
   db.user=root
   db.password=你的MySQL密码
   ```
   （若组内有共享 MySQL，把 `localhost` 换成该机器 IP，即可实现多人共用同一库 = 最简单的“同步”。）

4. 启动：
   - 命令行：`mvn -DskipTests package` → 把 `target/cloud_note.war` 放入 Tomcat 的 `webapps`，启动 Tomcat；
   - 或 IDE 配置 Tomcat 运行 `war exploded`。
   访问：`http://localhost:8080/cloud_note/`

---

## 三、方案 B：H2 嵌入式（免装 MySQL，最省事）

1. 克隆、用 Maven 导入 `cloud_note`（同上）；
2. **不需要建任何数据库**：首次启动时程序会在数据目录自动建库并导入种子数据（H2 文件库）；
3. 启动方式同方案 A（mvn package 或 IDE）；
4. 数据目录：默认是相对 Tomcat 启动目录的 `CloudNoteData` 文件夹，建议固定：
   - Tomcat 启动前加 JVM 参数指定数据目录：
     ```
     -Dcloudnote.data=D:\cloudnote-data
     ```
   - IDEA Tomcat 配置：Run → Edit Configurations → VM options 加上面参数；
   - 该目录下会自动生成 `cloud_note.mv.db` 和 `backup`（每日自动备份，最新版代码含该功能）。
   - ⚠️ 数据全在这个目录，删除即清空，别放在临时目录；换机器/换目录 = 数据不跟随（多人同步请看文档《数据持久化与同步说明.md》）。

---

## 四、通用信息

- 访问地址：`http://localhost:8080/cloud_note/`（端口被占则改 Tomcat `server.xml` 的 8080）
- 预置账号：
  | 账号 | 密码 | 角色 |
  |---|---|---|
  | admin | admin123 | 系统管理员（后台：用户/管理员/分享/活动管理） |
  | demo | 123456 | 普通用户 |
  - 登录页下方有「游客浏览」「管理员登录」入口（若看不到请 Ctrl+F5 强刷，避免旧缓存）。
- 修改代码后热部署/重启即可；前端页面改动后浏览器 Ctrl+F5。
- 管理员登录后左上菜单可回前台；普通用户工作台右上角菜单可“导出我的数据”（备份/未来同步用）。

## 五、常见问题

| 现象 | 处理 |
|---|---|
| 登录页没有“游客浏览/管理员登录” | 浏览器缓存旧页面 → Ctrl+F5 强刷一次 |
| 启动报“数据库初始化失败” | H2 模式别删数据目录里的库文件；MySQL 模式确认已导入 cloud_note.sql 且 jdbc.properties 已切换 |
| 端口 8080 被占用 | 停掉占用程序或改 Tomcat 端口；同时只启动一个实例 |
| 列表标题显示 undefined | 旧版本（9/2 版）在 H2 下的已知问题；拉最新代码即修复 |
| 页面样式/功能和我这边不一致 | 确保拉的是 `main` 最新代码（含 c53cd05 及后续提交）并重新打包 |
| 需要多人数据同步 | 看《数据持久化与同步说明.md》：推荐一台机器集中部署，或组内共享 MySQL |

## 六、版本对应（Git 提交）

| 提交 | 说明 |
|---|---|
| ef3b70c | 游客/管理员角色登录 + 基础后台（9/2） |
| c53cd05 | 分享分页全文搜索、收藏查看修复、管理员管理（9/3） |
| 66734d9（本地） | 数据自动备份/立即备份、用户数据导出接口（未推送时以 GitHub 实际为准） |

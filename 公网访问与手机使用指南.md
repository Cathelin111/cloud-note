# 云笔记（新版）公网访问与手机使用指南

> 场景：**不想和电脑连同一个 Wi‑Fi，也想在手机上打开云笔记新版**（比如用 4G/5G 流量、或在学校/别人家的网络里）。
> 结论：用**内网穿透（SSH 反向隧道）**，电脑不需要公网 IP、路由器不需要做端口映射。

---

## 一、最省事：一键脚本

1. 双击仓库根目录的 **`启动手机访问.bat`**（或在 PowerShell 里执行 `powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1`）。
2. 脚本会自动：检查/启动后端(8081) → 检查/启动前端(5173) → 建立公网隧道 → **打印手机访问网址**。
3. 手机（4G/5G 或任意 Wi‑Fi）浏览器打开打印出来的：

   ```
   https://xxxxxxxxxxxx.lhr.life
   ```

4. 网址同时写入仓库根目录 `手机访问地址.txt`，忘了可以翻这个文件。

**必须保持**：电脑开机 + 脚本窗口不关（关掉就断）。

演示账号：`admin / admin123`（管理员）、`demo / 123456`（普通用户）；游客不用登录，可直接看分享广场与活动。

### 脚本参数

```powershell
powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1              # 默认：localhost.run 隧道
powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1 -Tunnel pinggy  # 换 Pinggy 隧道
powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1 -NoStart        # 前后端已手动起好，只开隧道
powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1 -Stop           # 关闭公网访问
```

---

## 二、原理（为什么不用同一网络也行）

```
手机浏览器  ──https──▶  xxxxxxxx.lhr.life（公网服务器）
                              ▲
                              │  SSH 反向隧道：由本机主动连出去建立
                              │  （所以不需要公网 IP、不需要端口映射、能穿透运营商 CGNAT）
                       本机 5173  Vue 开发服务器
                              │  /api 反向代理
                       本机 8081  Spring Boot
                              │
                       本机 3306  MySQL
```

几个关键点：

| 点 | 说明 |
|---|---|
| 只暴露 5173 | 后端 8081、数据库 3306 **不开公网**，由前端 dev server 的 `/api` 代理转发，安全性更好 |
| 家庭宽带为什么直连不通 | 多数家宽是运营商大内网（CGNAT），外网访问不到家里的 IP；隧道是"由内向外"建连，能绕过 |
| 电脑要开着 | 隧道入口在你电脑上，电脑关机/睡眠/断网即失效 |
| 网址为什么会变 | 免费隧道每次重开都随机分配域名，以 `手机访问地址.txt` 为准 |

---

## 三、几种方案对比

| 方案 | 手机是否要同一网络 | 需要公网 IP / 端口映射 | 网址 | 有效期 | 适用 |
|---|---|---|---|---|---|
| 局域网直连 `http://172.28.161.7:5173` | **要** | 否 | 局域网 IP（换网络就变） | 电脑开着就行 | 课堂/家里演示 |
| **localhost.run**（脚本默认） | 不要 | 否 | 每次重开都变 | 连接期间（可能空闲掉线，重连即可） | **推荐，作业/演示** |
| Pinggy 免费隧道 | 不要 | 否 | 每次重开都变 | 免费版约 60 分钟/次 | 备用（脚本 `-Tunnel pinggy`） |
| Cloudflare 命名隧道 | 不要 | 否 | **可固定** | 长期 | 需要 Cloudflare 账号，适合长期演示 |
| 云服务器 + 域名 + Nginx | 不要 | 需要服务器 | **固定** | 长期 | 正式上线 |

局域网直连的做法（备用）：手机和电脑连同一个 Wi‑Fi，然后

```powershell
# 查看本机局域网 IP（WLAN 那一行）
ipconfig | findstr /i "IPv4"
# 放行防火墙 5173（只需一次，管理员 PowerShell）
netsh advfirewall firewall add rule name="cloudnote5173" dir=in action=allow protocol=TCP localport=5173
```

手机访问 `http://<上面的IPv4>:5173`。注意：新版的 Vite 已配置 `host: true`（监听所有网卡）和 `allowedHosts: true`（允许任意域名/IP 访问）。

---

## 四、手动启动（不想用脚本时）

```powershell
# 1) MySQL（本机服务）
net start mysql96

# 2) 后端：新版需要 JDK 17+，而本机默认 JAVA_HOME 是 1.8（旧系统用的），所以要显式指定
cd .\cloudnote-modern\backend
& "C:\Program Files\Java\jdk-25.0.2\bin\java.exe" -jar target\cloudnote-backend-0.1.0-SNAPSHOT.jar
#    改过后端代码则先构建： & "C:\Program Files\Java\jdk-25.0.2\bin\java.exe" 无关，用 Maven：
#    E:\cloudnote\_tools\apache-maven-3.9.9\bin\mvn.cmd clean package -DskipTests

# 3) 前端（另开一个窗口）
cd .\cloudnote-modern\frontend-web
npm run dev

# 4) 公网隧道（再开一个窗口，保持不关）
ssh -o StrictHostKeyChecking=accept-new -R 80:localhost:5173 nokey@localhost.run
#    输出里的 https://xxxx.lhr.life 就是手机访问地址
```

---

## 五、常见问题

| 现象 | 原因 / 处理 |
|---|---|
| 手机打开一直转圈、白屏 | 电脑上 5173 或隧道窗口已经关了；重跑脚本 |
| `502 Bad Gateway` | 后端 8081 没起来（看 `.tunnel\backend.err.log`，常见原因是 MySQL 没启动） |
| `Blocked request. This host is not allowed.` | Vite 的 host 校验；本项目已设 `allowedHosts: true`，若你改了配置需保留 |
| 手机能打开页面但接口全是红色报错 | 后端没起来，或 `/api` 代理没生效（确认前端是 `npm run dev` 起的，不是 `npx serve dist`） |
| 网址昨天还能用，今天打不开 | 免费隧道掉线/换域名了；重跑脚本，以 `手机访问地址.txt` 为准 |
| 想让网址固定不变 | 用 Cloudflare 命名隧道，或部署到云服务器 + 域名（见《部署指南》） |
| `ssh: command not found` | 启用 Windows 自带 OpenSSH 客户端：管理员 PowerShell 执行 `Add-WindowsCapability -Online -Name OpenSSH.Client~~~~0.0.1.0` |

---

## 六、公网暴露的安全提醒（演示前请做）

一旦把网站开到公网，任何人拿到网址都能访问，所以：

1. **改掉默认口令**：`admin/admin123`、`demo/123456` 是公开写在文档里的弱口令。
   - 登录后在页面里改，或直接改库：
     ```sql
     -- 密码是 BCrypt 哈希，建议在页面上改；这里仅示例删除演示账号
     DELETE FROM cn_user WHERE cn_user_name = 'demo';
     ```
2. **换掉 JWT 密钥**：`cloudnote-modern/backend/src/main/resources/application.yml` 里的 `jwt.secret`，改成一串足够长的随机字符（生产环境建议用环境变量覆盖）。
3. **不要额外暴露 8081 / 3306**：本方案只把 5173 映射出去，别再把后端或数据库单独做端口映射。
4. **演示完就关**：执行 `.\启动手机公网访问.ps1 -Stop`，或直接关掉脚本窗口。
5. 手机端分享出去的链接就等于把入口给出去了，注意别把管理后台的账号密码写在公开的地方。

---

## 七、相关文件

| 文件 | 说明 |
|---|---|
| `启动手机访问.bat` | 双击入口（转调下面的 ps1） |
| `启动手机公网访问.ps1` | 一键脚本本体（启动前后端 + 开隧道 + 打印地址） |
| `手机访问地址.txt` | 运行时生成，记录最近一次的公网地址（不入库） |
| `.tunnel/` | 运行时生成的日志与 PID 文件（不入库） |
| `cloudnote-modern/frontend-web/vite.config.js` | `host: true` / `allowedHosts: true` / `/api` 代理配置 |

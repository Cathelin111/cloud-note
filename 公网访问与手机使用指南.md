# 云笔记（新版）公网访问与手机使用指南

> 场景：**不想和电脑连同一个 Wi‑Fi，也想在手机上打开云笔记新版**（用 4G/5G 流量、或在学校/别人家的网络里）。
> 做法：**内网穿透**——电脑不需要公网 IP，路由器也不需要做端口映射。
> 默认方案用 **Cloudflare 快速隧道（cloudflared）**：手机打开**没有中间提示页**，实测最稳。

---

## 一、最省事：一键脚本

1. 双击仓库根目录的 **`启动手机访问.bat`**（或在 PowerShell 里执行 `powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1`）。
2. 脚本会自动：检查/启动后端(8081) → 检查/启动前端(5173) → 建立公网隧道 → **打印手机访问网址**；之后每 45 秒自检，掉线自动重建。
3. 手机（4G/5G 或任意 Wi‑Fi）浏览器**直接打开**打印出来的网址，形如：

   ```
   https://xxxx-xxxx-xxxx.trycloudflare.com
   ```

   不需要点任何“继续/Continue”。

4. 网址同时写入仓库根目录 `手机访问地址.txt`。

**必须保持**：电脑开机 + 脚本窗口不关（关掉就断）。

演示账号：`admin / admin123`（管理员）、`demo / 123456`（普通用户）；游客不用登录，可直接看分享广场与活动。

### 第一次使用需要 cloudflared（约 55MB，一次性）

脚本会自动在这些位置找 `cloudflared.exe`：

```
<仓库根>\.tools\cloudflared.exe
E:\cloudnote\_tools\cloudflared.exe
%LOCALAPPDATA%\cloudflared\cloudflared.exe
%ProgramFiles%\cloudflared\cloudflared.exe
%USERPROFILE%\cloudflared.exe
（以及 PATH 里的 cloudflared）
```

本机已经下载在 `E:\cloudnote\_tools\cloudflared.exe`，直接就能用。换电脑时下载（GitHub 直连不稳定，用了国内镜像）：

```powershell
New-Item -ItemType Directory -Force 'E:\cloudnote\_tools' | Out-Null
Invoke-WebRequest 'https://gh.llkk.cc/https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe' `
  -OutFile 'E:\cloudnote\_tools\cloudflared.exe'
```

### 脚本参数

```powershell
powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1                 # 默认：Cloudflare 隧道（推荐）
powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1 -Tunnel serveo  # 备选：serveo（需点一次 Continue to Site）
powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1 -Tunnel pinggy  # 备选：pinggy（有提示页，且国内 DNS 解析不到）
powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1 -Tunnel lhr     # 备选：localhost.run（实测不可用）
powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1 -NoStart        # 前后端已手动起好，只开隧道
powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1 -NoWatch        # 不做掉线守护
powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1 -Stop           # 关闭公网访问
```

---

## 二、原理（为什么不用同一网络也行）

```
手机浏览器  ──https──▶  xxxx.trycloudflare.com（Cloudflare 边缘节点）
                              ▲
                              │  隧道由本机主动连出去建立（出方向 TCP 443，走 http2）
                              │  → 不需要公网 IP、不需要端口映射、能穿透运营商 CGNAT
                       本机 5173  Vue 开发服务器
                              │  /api 反向代理
                       本机 8081  Spring Boot
                              │
                       本机 3306  MySQL
```

| 关键点 | 说明 |
|---|---|
| 只暴露 5173 | 后端 8081、数据库 3306 **不开公网**，由前端 dev server 的 `/api` 代理转发 |
| 为什么直连家庭宽带不行 | 本机 IPv4 是 `100.81.128.199`，属运营商大内网（CGNAT），外网连不进来；隧道由内向外建连可绕过 |
| 电脑要开着 | 隧道入口在你电脑上，关机/睡眠/断网即失效 |
| 网址会变 | 免费快速隧道每次重建都会分配新域名，以 `手机访问地址.txt` 为准 |
| 为什么指定 http2 | cloudflared 默认走 QUIC(UDP)，部分网络会卡在 “Requesting new quick Tunnel”；`--protocol http2` 用 TCP，更稳（脚本已内置） |

---

## 三、各方案实测结论（2026-09-10 实测）

| 方案 | 手机是否要同一网络 | 需要公网 IP/端口映射 | 网址 | 实测结论 |
|---|---|---|---|---|
| **Cloudflare 快速隧道（默认）** | 不要 | 否 | 每次重建变 | ✅ **可用**：首页/接口/登录/后台全部 200，手机打开**无提示页** |
| serveo | 不要 | 否 | 每次重建变 | ⚠️ 可用，但**手机首次打开会出现英文提示页**，需点 “Continue to Site”；实测运行约 10 分钟后掉线（公网 502） |
| Pinggy 免费隧道 | 不要 | 否 | 每次重建变 | ❌ 子域名在**国内公共 DNS 解析不到**（阿里/腾讯 DNS 均返回 NXDOMAIN），手机大概率打不开；另有提示页与 60 分钟限制 |
| localhost.run | 不要 | 否 | 每次重建变 | ❌ 实测不可用：SSH 连上、给了网址，但公网一直回 `no tunnel here` / 502 |
| **IPv6 直连**（见第五节） | 不要 | 不需要 IPv4 公网 | 本机 IPv6 地址 | ❓ 未验证（本机自测不算数，需手机实测） |
| 云服务器 + 域名 + Nginx | 不要 | 需服务器 | 可固定 | 长期方案，见《部署指南》 |

局域网直连（备用，手机和电脑连同一个 Wi‑Fi）：

```powershell
ipconfig | findstr /i "IPv4"     # 看 WLAN 那行的地址
# 放行防火墙 5173（本机已经加过这条规则）
netsh advfirewall firewall add rule name="cloudnote5173" dir=in action=allow protocol=TCP localport=5173
```

---

## 四、手动启动（不想用脚本时）

```powershell
# 1) MySQL（本机服务）
net start mysql96

# 2) 后端：新版需要 JDK 17+，而本机默认 JAVA_HOME 是 1.8（旧系统用的），所以要显式指定
cd .\cloudnote-modern\backend
& "C:\Program Files\Java\jdk-25.0.2\bin\java.exe" -jar target\cloudnote-backend-0.1.0-SNAPSHOT.jar
#    改过后端代码则先构建：E:\cloudnote\_tools\apache-maven-3.9.9\bin\mvn.cmd clean package -DskipTests

# 3) 前端（另开一个窗口）
cd .\cloudnote-modern\frontend-web
npm run dev

# 4) 公网隧道（再开一个窗口，保持不关）
E:\cloudnote\_tools\cloudflared.exe tunnel --url http://localhost:5173 --no-autoupdate --protocol http2
#    输出框里的 https://xxxx.trycloudflare.com 就是手机访问地址
```

---

## 五、进阶：IPv6 直连（没有提示页，但要手机实测）

本机**有公网 IPv6**（`ipconfig` 里 `2409:8a20:6:326b:975a:ab27:de78:dd3b`），4G/5G 一般也有 IPv6，
理论上手机可以直接访问 `http://[2409:8a20:6:326b:975a:ab27:de78:dd3b]:5173`，**不需要任何隧道和第三方**。

已确认满足的前置条件：

- Vite 监听所有网卡：`netstat` 里有 `[::]:5173 LISTENING`（`vite.config.js` 已设 `host: true`）
- 防火墙放行：已有入站规则 `cloudnote5173`，TCP 5173，Domain/Private/Public 全开

未验证的一点：**光猫/路由器是否允许外网主动连进家里的 IPv6**（很多默认拦截）。手机实测方法：

1. 手机关 Wi‑Fi、开移动数据（确认手机有 IPv6）
2. 打开 `http://[2409:8a20:6:326b:975a:ab27:de78:dd3b]:5173`
3. 能打开 → 以后就用它（注意 IPv6 地址可能随宽带重播变化）

缺点：纯 HTTP 不加密、地址会变、部分网络拦截入站 IPv6；只适合临时测试。

---

## 六、常见问题

| 现象 | 原因 / 处理 |
|---|---|
| 手机显示 `no tunnel here :(` | 用的是 localhost.run 隧道（该项目前不可用），改默认 Cloudflare：直接重跑脚本 |
| 手机显示 502 / `Cloudflare Tunnel error` | 隧道进程掉了或前端 5173 没起来；脚本守护会自动重建，也可重跑脚本 |
| 手机打开一直转圈 | 电脑上 5173 或脚本窗口已经关了 |
| 手机首次打开是英文提示页 | 只出现在 serveo/pinggy 隧道；点 **Continue to Site**，或改用默认的 Cloudflare 隧道 |
| 手机上接口全报错 | 后端 8081 没起来（看 `.tunnel\backend.err.log`，常见原因是 MySQL 没启动） |
| `Blocked request. This host is not allowed.` | Vite 的 host 校验；本项目已设 `allowedHosts: true` |
| cloudflared 卡在 `Requesting new quick Tunnel` | 网络屏蔽 QUIC；用 `--protocol http2`（脚本已内置） |
| `ssh: command not found` | 管理员 PowerShell：`Add-WindowsCapability -Online -Name OpenSSH.Client~~~~0.0.1.0` |
| 想要固定网址 | 部署到云服务器 + 域名（见《部署指南》），或用 Cloudflare 命名隧道（需账号） |

---

## 七、公网暴露的安全提醒（演示前请做）

1. **改掉默认口令**：`admin/admin123`、`demo/123456` 是写在文档里的弱口令，公网一开谁拿到网址都能试。
2. **换掉 JWT 密钥**：`cloudnote-modern/backend/src/main/resources/application.yml` 里的 `jwt.secret`。
3. **不要额外暴露 8081 / 3306**：本方案只映射 5173，别再把后端或数据库单独做端口映射。
4. **演示完就关**：`.\启动手机公网访问.ps1 -Stop`。
5. 免费隧道服务商会终止 HTTPS 看到你的流量，不要用它传敏感数据。

---

## 八、相关文件

| 文件 | 说明 |
|---|---|
| `启动手机访问.bat` | 双击入口（转调下面的 ps1） |
| `启动手机公网访问.ps1` | 一键脚本本体（启动前后端 + 开隧道 + 自检守护 + 打印地址） |
| `手机访问地址.txt` | 运行时生成，记录最近一次的公网地址（不入库） |
| `.tunnel/` | 运行时生成的日志与 PID 文件（不入库） |
| `cloudnote-modern/frontend-web/vite.config.js` | `host: true` / `allowedHosts: true` / `/api` 代理配置 |

# 云笔记（新版）公网访问与手机使用指南

> 场景：**不想和电脑连同一个 Wi‑Fi，也想在手机上打开云笔记新版**（用 4G/5G 流量、或在学校/别人家的网络里）。
> 做法：**内网穿透（SSH 反向隧道）**——电脑不需要公网 IP，路由器也不需要做端口映射。

---

## 一、最省事：一键脚本

1. 双击仓库根目录的 **`启动手机访问.bat`**（或在 PowerShell 里执行 `powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1`）。
2. 脚本会自动：检查/启动后端(8081) → 检查/启动前端(5173) → 建立公网隧道 → **打印手机访问网址**，并每隔 45 秒自检，掉线自动重建。
3. 手机（4G/5G 或任意 Wi‑Fi）浏览器打开打印出来的网址，形如：

   ```
   https://xxxxxxxxxxxx-112-0-62-148.serveousercontent.com
   ```

4. ⚠️ **手机第一次打开会先出现一个英文提示页**（Serveo / Pinggy 免费版的防钓鱼提示），点一下页面上的 **“Continue to Site”** 就进站了；点过一次后同一浏览器不再出现。
5. 网址同时写入仓库根目录 `手机访问地址.txt`，忘了可以翻这个文件。

**必须保持**：电脑开机 + 脚本窗口不关（关掉就断）。

演示账号：`admin / admin123`（管理员）、`demo / 123456`（普通用户）；游客不用登录，可直接看分享广场与活动。

### 脚本参数

```powershell
powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1                # 默认：serveo 隧道（无时长限制）
powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1 -Tunnel pinggy # 换 Pinggy 隧道（免费版约 60 分钟一次）
powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1 -Tunnel lhr    # 换 localhost.run（无提示页，但实测约 20 分钟掉线）
powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1 -NoStart       # 前后端已手动起好，只开隧道
powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1 -NoWatch       # 不做掉线守护
powershell -ExecutionPolicy Bypass -File .\启动手机公网访问.ps1 -Stop          # 关闭公网访问
```

---

## 二、原理（为什么不用同一网络也行）

```
手机浏览器  ──https──▶  xxxxxxxx.serveousercontent.com（公网服务器）
                              ▲
                              │  SSH 反向隧道：由本机主动连出去建立
                              │  （所以不需要公网 IP、不需要端口映射、能穿透运营商 CGNAT）
                       本机 5173  Vue 开发服务器
                              │  /api 反向代理
                       本机 8081  Spring Boot
                              │
                       本机 3306  MySQL
```

| 关键点 | 说明 |
|---|---|
| 只暴露 5173 | 后端 8081、数据库 3306 **不开公网**，由前端 dev server 的 `/api` 代理转发 |
| 为什么直连家庭宽带不行 | 本机 IPv4 是 `100.81.128.199`，属于运营商大内网（CGNAT），外网访问不到；隧道由内向外建连可绕过 |
| 电脑要开着 | 隧道入口在你电脑上，关机/睡眠/断网即失效 |
| 网址会变 | 免费隧道每次重建都会分配新域名，以 `手机访问地址.txt` 为准 |

---

## 三、几种方案对比（含实测结论）

| 方案 | 手机是否要同一网络 | 需要公网 IP/端口映射 | 网址 | 有效期 | 实测结论 |
|---|---|---|---|---|---|
| **serveo**（脚本默认） | 不要 | 否 | 每次重建变 | 连接期间 | ✅ 可用；手机首次打开有 “Continue to Site” 提示页 |
| Pinggy 免费隧道 | 不要 | 否 | 每次重建变 | 免费版约 60 分钟/次 | ✅ 可用；同样有提示页 |
| localhost.run | 不要 | 否 | 每次重建变 | 连接期间 | ⚠️ 实测约 20 分钟后服务端掉线（SSH 还活着但公网返回 `no tunnel here`） |
| **IPv6 直连**（见第五节） | 不要 | 不需要 IPv4 公网 | 本机 IPv6 地址 | 地址可能随宽带重播变化 | ❓ 未验证（本机测不算数，需手机实测） |
| Cloudflare 隧道 / 云服务器 + 域名 | 不要 | 否 / 需服务器 | 可固定 | 长期 | ❌ 本机网络直连 Cloudflare 边缘被阻断，cloudflared 取不到隧道 |

局域网直连（备用，手机和电脑连同一个 Wi‑Fi）：

```powershell
ipconfig | findstr /i "IPv4"     # 看 WLAN 那行的地址
# 放行防火墙 5173（只需一次，管理员 PowerShell；本机已经加过这条规则）
netsh advfirewall firewall add rule name="cloudnote5173" dir=in action=allow protocol=TCP localport=5173
```

---

## 四、手动启动（不想用脚本时）

```powershell
# 1) MySQL（本机服务）
net start mysql96

# 2) 后端：新版需要 JDK 17+，而本机默认 JAVA_HOME 是 1.8（旧系统用的），所以要显式指定
cd .\cloudnote-modern\backend
& "C:\Program Files\Java\jdk-25.0.2\bin\java.exe" -jar target\cloudnote-backend-0.1.0-SNAPSHOT.jar
#    改过后端代码则先构建：E:\cloudnote\_tools\apache-maven-3.9.9\bin\mvn.cmd clean package -DskipTests

# 3) 前端（另开一个窗口）
cd .\cloudnote-modern\frontend-web
npm run dev

# 4) 公网隧道（再开一个窗口，保持不关）
ssh -o StrictHostKeyChecking=accept-new -R 80:localhost:5173 serveo.net
#    输出里的 https://xxxx.serveousercontent.com 就是手机访问地址
```

---

## 五、进阶：IPv6 直连（没有提示页，但要手机实测）

本机**有公网 IPv6**（`ipconfig` 里 `2409:8a20:6:326b:975a:ab27:de78:dd3b`），而 4G/5G 一般也有 IPv6，
所以理论上手机可以直接访问 `http://[2409:8a20:6:326b:975a:ab27:de78:dd3b]:5173`，**不需要任何隧道**。

前置条件（已确认两项目前都满足）：

- Vite 监听所有网卡：`netstat` 里有 `[::]:5173 LISTENING`（`vite.config.js` 已设 `host: true`）
- 防火墙放行：已有入站规则 `cloudnote5173`，TCP 5173，Domain/Private/Public 全开

还没验证的一点：**光猫/路由器是否允许外网主动连进家里的 IPv6**（很多默认拦截）。请在手机上用流量试一次：

1. 手机关 Wi‑Fi，开移动数据（确认手机有 IPv6）
2. 浏览器打开 `http://[2409:8a20:6:326b:975a:ab27:de78:dd3b]:5173`
3. 能打开 → 以后都可以用这个地址（注意 IPv6 地址可能随宽带重播变化）

缺点：纯 HTTP 不加密、地址会变、部分网络拦截入站 IPv6，所以它只适合临时测试；要长期稳定还是云服务器 + 域名最靠谱。

---

## 六、常见问题

| 现象 | 原因 / 处理 |
|---|---|
| 手机显示 `no tunnel here :(` | 隧道在服务端掉了（`localhost.run` 常见）。脚本守护会自动重建并打印新网址；也可手动重跑脚本 |
| 手机打开一直转圈、白屏 | 电脑上 5173 或脚本窗口已经关了 |
| 手机打开是一个英文提示页 | 免费隧道防钓鱼提示，点 **Continue to Site** 即可（只出现一次） |
| `502 Bad Gateway` | 后端 8081 没起来（看 `.tunnel\backend.err.log`，常见原因是 MySQL 没启动） |
| `Blocked request. This host is not allowed.` | Vite 的 host 校验；本项目已设 `allowedHosts: true` |
| 手机能打开页面但接口报错 | 后端没起来，或前端不是用 `npm run dev` 起的（`dist` 静态托管不会有 `/api` 代理） |
| `ssh: command not found` | 管理员 PowerShell：`Add-WindowsCapability -Online -Name OpenSSH.Client~~~~0.0.1.0` |
| 想要固定网址 | 用 Cloudflare 命名隧道，或部署到云服务器 + 域名（见《部署指南》） |

---

## 七、公网暴露的安全提醒（演示前请做）

1. **改掉默认口令**：`admin/admin123`、`demo/123456` 是写在文档里的弱口令，公网一开谁拿到网址都能试。
2. **换掉 JWT 密钥**：`cloudnote-modern/backend/src/main/resources/application.yml` 里的 `jwt.secret`。
3. **不要额外暴露 8081 / 3306**：本方案只映射 5173，别再把后端或数据库单独做端口映射。
4. **演示完就关**：`.\启动手机公网访问.ps1 -Stop`。
5. 免费隧道服务商会看到你的访问流量（HTTPS 在服务商处终止），不要用它传敏感数据。

---

## 八、相关文件

| 文件 | 说明 |
|---|---|
| `启动手机访问.bat` | 双击入口（转调下面的 ps1） |
| `启动手机公网访问.ps1` | 一键脚本本体（启动前后端 + 开隧道 + 自检守护 + 打印地址） |
| `手机访问地址.txt` | 运行时生成，记录最近一次的公网地址（不入库） |
| `.tunnel/` | 运行时生成的日志与 PID 文件（不入库） |
| `cloudnote-modern/frontend-web/vite.config.js` | `host: true` / `allowedHosts: true` / `/api` 代理配置 |

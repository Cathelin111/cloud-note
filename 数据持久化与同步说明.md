# 数据持久化与跨设备同步说明

本文说明云笔记的数据保存在哪里、如何保证不丢失，以及为“多设备数据同步”预留的接口与未来方案。

## 一、数据保存在哪里（重要）

| 启动方式 | 数据目录 | 说明 |
|---|---|---|
| `云笔记.exe` 启动器（便携版） | `%USERPROFILE%\CloudNoteData`（如 `C:\Users\你的用户名\CloudNoteData`） | 主用方式，数据随用户目录，卸载/重装系统不丢 |
| `start_local.bat` | `%USERPROFILE%\CloudNoteData` | 已统一到与启动器同一目录 |
| 自动化冒烟测试 | 独立的临时目录（`_testdata` 等） | **只用于测试，会自动重建，绝不存放正式数据** |

> 数据目录约定：**固定一个目录、永不自动重置、备份放它下面的 `backup`**。
> 若某台电脑对用户目录不可写/不可靠（例如受限环境），只需修改 `start_local.bat` 里的
> `set DATADIR=...` 指向任意固定可写目录（如 `E:\cloudnote\CloudNoteData`），其余机制完全不变。

> 每个用户目录下只有一个 `cloud_note.mv.db`（H2 文件库），它就是“这台电脑上的全部数据”。
> 请不要删除/移动它；换电脑 ≠ 换数据（见第三节同步方案）。

## 二、如何保证数据不丢

1. **每日自动备份**：每次启动应用时，若当天还没有备份，会自动在
   `数据目录\backup\cloud_note_YYYYMMDD.sql` 生成一份**完整数据库快照**（在线 SCRIPT 导出，含全部表结构与数据）。
   只保留最近 15 份，更早的自动清理。
2. **手动立即备份**：后台管理 →「管理员管理」→「立即备份数据库」按钮（或接口
   `POST /admin/system/backup.do`，携带管理员 userId），生成带时分秒的备份文件。
3. **用户级导出**：登录后右上角头像菜单 →「导出我的数据」，下载当前账号的完整 JSON
   （用户档案 + 笔记本 + 笔记 + 分享 + 活动投稿），可自己留档。
4. **恢复方法**（误删/换机迁移文件库）：
   - 停止服务（`stop_local.bat` 或启动器“停止服务”）；
   - 把损坏/想替换的 `cloud_note.mv.db` 移走（可先备份）；
   - 用 H2 工具把最新快照导入：
     ```
     java -cp <h2-1.4.200.jar 路径> org.h2.tools.RunScript \
       -url "jdbc:h2:file:<数据目录>\cloud_note" -user sa -password "" \
       -script "<数据目录>\backup\cloud_note_20260903.sql"
     ```
     （jar 在 `webapps\cloud_note\WEB-INF\lib\h2-1.4.200.jar`）
   - 重新启动服务即可。

## 三、多设备同步：预留接口与演进路线

当前版本为“单机 H2 文件库”，已在代码中预留了同步所需的接口与文档约定：

### 已提供（v1 预留接口）

- `POST /data/export.do?userId=xxx`：导出指定用户的全部数据（JSON，含 `exportVersion` 字段）。
  前端入口：我的笔记 → 头像菜单 →「导出我的数据」。
- 导出 JSON 中所有主键均为稳定的 UUID，未来另一台设备“导入+按 ID 合并”即可增量同步；
  导入接口与冲突合并策略将在同步版本实现（规划路径 `/data/import.do` + 服务端合并）。

### 建议的同步演进路线（任选其一，按团队条件）

1. **集中部署（最简单，推荐演示）**
   选一台能长时间开机的电脑运行服务（本机或组员机），其他设备浏览器访问
   `http://<那台电脑的IP>:8080/cloud_note`。数据只有服务器上一份 → 天然持久、天然同步。
   （防火墙需放行 8080；演示时开即可。）

2. **共享 MySQL（真·多实例同库）**
   项目原生支持 MySQL：任一台机器装好 MySQL（或使用学校/组内已有的），
   修改 `cloud_note/src/main/resources/jdbc.properties` 指向该 MySQL 的 IP/库，
   首次导入 `cloud_note.sql`；各设备部署都连同一数据库，任意设备写入立即可见。
   H2→MySQL 的代码零改动（MyBatis 层已适配双库）；`jdbc.properties` 即“同步开关”。

3. **云服务器 / 云数据库**
   把 war 部署到云服务器（或直连云 MySQL/PostgreSQL），任何地点访问同一地址即同步；
   需要联网与云资源，团队可用时再实施。

## 四、给开发/测试人员的提醒

- 自动化冒烟脚本（`_smoke_*.ps1`）会**重建它们自己的临时库**，不要把它们指向正式数据目录；
- 任何“重置数据”操作前先确认目标目录不是 `%USERPROFILE%\CloudNoteData`；
- 改完代码重新打包部署后，浏览器记得 Ctrl+F5 强刷。

# 协作者使用指南（如何用一键上传脚本）

本仓库已配置一键上传脚本：**`push-github.bat`**（双击运行）+ **`push-github.ps1`**（主脚本）。

## 第一次使用（只需一次）

### 1. 安装 Git
下载安装：https://git-scm.com/download/win
安装时全部默认选项即可。

### 2. 生成自己的 SSH 密钥（用自己的 GitHub 账号）
打开命令行（开始菜单搜索 `cmd` 或 `PowerShell`），运行：

```
ssh-keygen -t ed25519 -C "你的GitHub邮箱"
```

一路回车即可（不用设密码）。然后查看公钥：

```
type %USERPROFILE%\.ssh\id_ed25519.pub
```

复制输出的整行内容（以 `ssh-ed25519 AAAA...` 开头）。

### 3. 把公钥添加到自己的 GitHub 账号
- 打开 https://github.com/settings/keys
- 点 **New SSH key**
- Title 随意填，Key 粘贴上一步复制的内容
- 点 **Add SSH key**

### 4. 克隆本仓库（二选一）
```
git clone git@github.com:Cathelin111/cloud-note.git
```
克隆完成后进入文件夹：
```
cd cloud-note
```

## 日常使用（每次改完代码）

在 `cloud-note` 文件夹里**双击 `push-github.bat`**，脚本会自动：
1. 检测改动
2. 暂存所有改动（git add -A）
3. 自动提交（带时间戳）
4. 推送到 GitHub

想写自定义提交说明，在命令行运行：
```
powershell -ExecutionPolicy Bypass -File push-github.ps1 -Message "你的改动说明"
```

## 常见问题

| 问题 | 解决 |
|------|------|
| 双击后提示找不到 powershell | 用新开的命令行窗口运行，或用上面第 4 步的命令方式 |
| `Permission denied (publickey)` | 第 2、3 步没做对，检查公钥是否添加到**你自己的** GitHub 账号 |
| 推送被拒绝 / 自动同步失败 | 可能有冲突，按脚本提示手动处理：先 `git pull` 再 `git push` |
| 提示不是 git 仓库 | 确认是在 `cloud-note` 文件夹里运行脚本 |

## 注意
- 每个人的 SSH 密钥是私有的，**不要共享私钥文件**（id_ed25519 不带 .pub 的那个）。
- 提交记录会显示你自己的 GitHub 用户名，这是正常的。

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudnote.mapper.NoteMapper">

    <select id="findByBookId" parameterType="string" resultType="com.cloudnote.entity.Note">
        select cn_note_id, cn_note_title
        from cn_note
        where cn_notebook_id = #{bookId} and cn_note_status_id = '1'
        order by cn_note_last_modify_time desc
    </select>

    <select id="countByBookId" parameterType="string" resultType="int">
        select count(*) from cn_note where cn_notebook_id = #{bookId} and cn_note_status_id = '1'
    </select>

    <select id="findByNoteId" parameterType="string" resultType="com.cloudnote.entity.Note">
        select * from cn_note where cn_note_id = #{noteId}
    </select>

    <insert id="insert" parameterType="com.cloudnote.entity.Note">
        insert into cn_note(
            cn_note_id, cn_notebook_id, cn_user_id, cn_note_status_id, cn_note_type_id,
            cn_note_title, cn_note_body, cn_note_create_time, cn_note_last_modify_time)
        values(
            #{cn_note_id}, #{cn_notebook_id}, #{cn_user_id}, #{cn_note_status_id}, #{cn_note_type_id},
            #{cn_note_title}, #{cn_note_body}, #{cn_note_create_time}, #{cn_note_last_modify_time})
    </insert>

    <update id="updateContent" parameterType="map">
        update cn_note set
            cn_note_title = #{title},
            cn_note_body = #{body},
            cn_note_last_modify_time = #{lastModify}
        where cn_note_id = #{noteId}
    </update>

    <update id="move" parameterType="map">
        update cn_note set cn_notebook_id = #{bookId} where cn_note_id = #{noteId}
    </update>

    <delete id="deleteById" parameterType="string">
        delete from cn_note where cn_note_id = #{noteId}
    </delete>
</mapper>

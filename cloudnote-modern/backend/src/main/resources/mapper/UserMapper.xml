<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudnote.mapper.UserMapper">

    <select id="findByName" parameterType="string" resultType="com.cloudnote.entity.User">
        select * from cn_user where cn_user_name = #{name}
    </select>

    <select id="findById" parameterType="string" resultType="com.cloudnote.entity.User">
        select * from cn_user where cn_user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.cloudnote.entity.User">
        insert into cn_user(
            cn_user_id, cn_user_name, cn_user_password, cn_user_token, cn_user_nick,
            cn_user_role, cn_user_status, cn_user_create_time)
        values(
            #{cn_user_id}, #{cn_user_name}, #{cn_user_password}, #{cn_user_token}, #{cn_user_nick},
            #{cn_user_role}, #{cn_user_status}, #{cn_user_create_time})
    </insert>

    <update id="updatePassword" parameterType="map">
        update cn_user set cn_user_password = #{password} where cn_user_id = #{userId}
    </update>

    <update id="updateStatus" parameterType="map">
        update cn_user set cn_user_status = #{status} where cn_user_id = #{userId}
    </update>

    <!-- ============ 管理员后台 ============ -->
    <select id="findAdminPage" parameterType="map" resultType="com.cloudnote.entity.User">
        select * from cn_user
        where cn_user_role = 'user'
        <if test="keyword != null and keyword != ''">
            and (cn_user_name like #{keyword} or COALESCE(cn_user_nick, '') like #{keyword})
        </if>
        order by cn_user_name
        limit #{pageSize} offset #{begin}
    </select>

    <select id="countAdmin" parameterType="string" resultType="int">
        select count(*) from cn_user
        where cn_user_role = 'user'
        <if test="keyword != null and keyword != ''">
            and (cn_user_name like #{keyword} or COALESCE(cn_user_nick, '') like #{keyword})
        </if>
    </select>

    <update id="updateRole" parameterType="map">
        update cn_user set cn_user_role = #{role} where cn_user_id = #{userId}
    </update>

    <select id="findAdmins" resultType="com.cloudnote.entity.User">
        select * from cn_user where cn_user_role = 'admin' order by cn_user_name
    </select>

    <delete id="deleteById" parameterType="string">
        delete from cn_user where cn_user_id = #{userId}
    </delete>
</mapper>
